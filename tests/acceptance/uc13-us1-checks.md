# UC-13 US1 Checks

- Admin can start schedule generation (`202`).
- Concurrent generation request is rejected (`409`).
- Run status polling returns run information.
- Generated schedule list/detail endpoints return assignments.
- Non-admin caller is rejected (`403`).
